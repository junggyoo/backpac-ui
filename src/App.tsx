import styled from 'styled-components';

export default function App() {
  return (
    <AppWrapper>
      app
      <CardUIView></CardUIView>
    </AppWrapper>
  );
}

const AppWrapper = styled.main``;

const CardUIView = styled.section``;
